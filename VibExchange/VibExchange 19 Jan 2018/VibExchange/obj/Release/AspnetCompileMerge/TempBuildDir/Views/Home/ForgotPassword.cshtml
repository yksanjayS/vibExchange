@model VibExchange.Models.ForgotPassword

@{
    ViewBag.Title = "Forgot Password";
    Layout = "~/Views/Shared/_VibExchangeLayout.cshtml";
}
<div class="container row-fluid">
    <h4 style="color: chocolate">Forgot Password >> </h4>
    <div class="row-fluid">
        <div class="col-md-8">
            <section id="loginForm">
                @using (Html.BeginForm("ForgotPassword", "Home", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
                {
                    @Html.AntiForgeryToken()
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    <div class="form-group">
                        @Html.LabelFor(m => m.EmailID, new { @class = "col-md-2 control-label" })
                        <div class="col-md-10">
                            @Html.TextBoxFor(m => m.EmailID, new { @class = "form-control" })
                            @Html.ValidationMessageFor(m => m.EmailID, "", new { @class = "text-danger" })
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Submit" class="btn btn-default" />
                        </div>
                    </div>
                }
            </section>
        </div>
    </div>

    <div>
        @Html.ActionLink("Back to Login", "Login")
    </div>
    </div>
    @section Scripts {
        @Scripts.Render("~/bundles/jqueryval")
    }
